{
  "name": "boost-tai-ctsi-automation",
  "version": "1.2.2",
  "private": true,
  "description": "Download merged Boost invoices from TAI, split by cover page, upload to CTSI",
  "author": "Vantage CFO",
  "type": "commonjs",
  "main": "src/main/main.js",
  "scripts": {
    "start": "electron .",
    "dist": "electron-builder --win",
    "postinstall": "playwright install chromium",
    "test": "echo \"'npm test' is disabled: the Playwright suite runs a LIVE end-to-end workflow that logs into TAI/CTSI with real credentials and performs REAL production invoice uploads to CTSI. Run 'npm run test:e2e-live' only if you truly intend to hit production.\"",
    "test:e2e-live": "playwright test"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1",
    "pdfjs-dist": "^6.1.200",
    "playwright": "^1.61.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "dotenv": "^17.4.2",
    "electron": "^33.0.0",
    "electron-builder": "^25.0.0"
  }
}
